machine:
  php:
    version: 7.0.4
  environment:
    COVERALLS_REPO_TOKEN: uFuGf8IAEJdcMKHoLYE0SLPSWM0qtAkRA
test:
  override:
    - vendor/bin/phpunit
  dependencies:
  override:
    - mkdir -p build/logs
    - composer install --dev --no-interaction
    - sed -i 's/^;//' ~/.phpenv/versions/$(phpenv global)/etc/conf.d/xdebug.ini